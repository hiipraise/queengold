import { categories, collections, products, promotions } from "@/lib/site-data";

export default function CommerceAdminPage() {
  const inventory = products.reduce((sum, product) => sum + product.stock, 0);
  return <div className="space-y-6"><div><h1 className="font-serif text-2xl text-gold-gradient">Commerce Dashboard</h1><p className="mt-1 text-xs tracking-wide text-[var(--text-muted)]">Products, collections, inventory, promotions, orders, and payments summary.</p></div><div className="grid gap-4 md:grid-cols-4">{[{label:'Products',value:products.length},{label:'Categories',value:categories.length},{label:'Collections',value:collections.length},{label:'Inventory',value:inventory}].map((stat) => <div key={stat.label} className="card-luxury p-5"><p className="text-xs uppercase tracking-[0.22em] text-[var(--gold-muted)]">{stat.label}</p><p className="mt-2 font-serif text-3xl">{stat.value}</p></div>)}</div><div className="grid gap-6 lg:grid-cols-2"><section className="card-luxury p-6"><h2 className="font-serif text-xl">Featured content & catalog</h2><div className="mt-4 space-y-3">{products.map((product) => <div key={product.slug} className="flex items-center justify-between rounded-xl border border-[var(--border-gold)] px-4 py-3"><div><p className="font-serif text-lg">{product.name}</p><p className="text-xs uppercase tracking-[0.18em] text-[var(--text-muted)]">{product.collection} · {product.category}</p></div><span className="text-xs uppercase tracking-[0.18em] text-[var(--gold-muted)]">Stock {product.stock}</span></div>)}</div></section><section className="space-y-6"><div className="card-luxury p-6"><h2 className="font-serif text-xl">Promotions</h2><div className="mt-4 space-y-3">{promotions.map((promo) => <div key={promo.code} className="rounded-xl border border-[var(--border-gold)] p-4"><p className="text-xs uppercase tracking-[0.18em] text-[var(--gold-muted)]">{promo.code}</p><p className="mt-2 font-serif text-lg">{promo.title}</p><p className="mt-2 text-sm text-[var(--text-muted)]">{promo.detail}</p></div>)}</div></div><div className="card-luxury p-6"><h2 className="font-serif text-xl">Payments & orders</h2><p className="mt-3 text-sm text-[var(--text-muted)]">Squad initialization, order capture, and verification endpoints are scaffolded to connect payments with digital passport fulfillment.</p></div></section></div></div>;
}
