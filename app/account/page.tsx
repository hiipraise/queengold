import Link from "next/link";
import { customer } from "@/lib/site-data";

export default function AccountPage() {
  return <main className="mx-auto max-w-7xl space-y-8 px-4 py-10 sm:px-6 lg:px-8"><section><p className="text-xs uppercase tracking-[0.35em] text-[var(--gold-muted)]">Customer account</p><h1 className="font-serif text-5xl">Welcome back, {customer.name.split(' ')[0]}.</h1><p className="mt-4 text-sm text-[var(--text-muted)]">View order history, saved addresses, and your authenticity-first ownership timeline.</p></section><section className="grid gap-6 lg:grid-cols-[1fr_1fr]"><div className="card-luxury p-6"><h2 className="font-serif text-3xl">Order History</h2><div className="mt-4 space-y-3">{customer.orders.map((order) => <div key={order.id} className="rounded-[1.5rem] border border-[var(--border-gold)] p-4"><div className="flex items-center justify-between"><p className="font-serif text-xl">{order.id}</p><span className="text-xs uppercase tracking-[0.22em] text-[var(--gold-muted)]">{order.status}</span></div><p className="mt-2 text-sm text-[var(--text-muted)]">{order.date} · {order.items} item · ${order.total.toLocaleString()}</p></div>)}</div></div><div className="card-luxury p-6"><h2 className="font-serif text-3xl">Saved Addresses</h2><div className="mt-4 space-y-3">{customer.addresses.map((address) => <div key={address.label} className="rounded-[1.5rem] border border-[var(--border-gold)] p-4"><p className="text-xs uppercase tracking-[0.22em] text-[var(--gold-muted)]">{address.label}</p><p className="mt-2 text-sm text-[var(--gold-light)]">{address.line1}, {address.city}, {address.state}, {address.country}</p></div>)}</div><Link href="/wishlist" className="mt-6 inline-flex text-xs uppercase tracking-[0.22em] text-[var(--gold-light)]">Open wishlist</Link></div></section></main>;
}
